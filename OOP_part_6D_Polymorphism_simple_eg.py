# To understnad polymorphism, let's look at "len" built-in function first.

name = "Donghun"
print(len(name)) # show 6

some_list = ["some","name"]
print(len(some_list)) # show 2

# this shows that a single entity "len" knows how to handle diff types of data structures

